export type reponseType = {
    id: number,
    name: string,
    email: string,
    age: number,
    isEmployee: boolean,
    country: string,
    isError: boolean
}

export type usersType = {
    id: number,
    name: string,
    email: string,
    age: number,
    isEmployee: boolean,
    country: string,
    isError: boolean
}